import React from 'react';
import ReactDom from 'react-dom';
import TestUtils from 'react-addons-test-utils';
import expect from 'expect';
import $ from 'jQuery';


import Camper from 'Camper';

describe('Camper', () => {
  it('should exist', () => {
    expect(Camper).toExist();
  });
});
